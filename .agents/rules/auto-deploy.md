---
trigger: always_on
---

After every code change (fix, feat, or chore), always run: `git add -A && git commit -m "<type>: <description>" && git push origin main`. Confirm to the user with the commit hash after a successful push.
